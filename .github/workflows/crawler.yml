# Version: v1.3
# GitHub Actions 自动运行网页爬虫
# 每天北京时间 08:00 运行
# 保留最近15天结果

name: Run Crawler

on:
  workflow_dispatch:
  schedule:
    # GitHub Actions 使用 UTC 时间
    # UTC 00:00 = 北京时间 08:00
    - cron: "0 0 * * *"

permissions:
  contents: write

jobs:
  crawler:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4 lxml

      - name: Run crawler
        run: |
          python crawler.py

      - name: Remove results older than 15 days
        run: |
          find results -mindepth 1 -maxdepth 1 -type d -mtime +15 -exec rm -rf {} +

      - name: Commit results
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add results/
          git commit -m "Update crawler results and cleanup old data" || echo "No changes to commit"
          git push
